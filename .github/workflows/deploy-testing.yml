name: gcloud

on: [push]

jobs:
  deploy-testing:
    runs-on: ubuntu-latest
    name: deploy-testing
    steps:
      - name: Retrieving the repo
        uses: actions/checkout@v1
      - name: Edit app.yaml
        uses: mikefarah/yq@3.2.1
        with:
          cmd: yq w -i app.yaml env_variables.HISTORY_URL ${{secrets.HISTORY_URL}}
        uses: mikefarah/yq@3.2.1
        with:
          cmd: yq w -i app.yaml env_variables.DEVICE_CONTROL_URL ${{secrets.DEVICE_CONTROL_URL}}
            # 
            # yq w -i app.yaml env_variables.USER_URL ${{secrets.USER_URL}}
            # yq w -i app.yaml env_variables.JWT_KEY ${{secrets.JWT_KEY}}
            # yq w -i app.yaml env_variables.GOOGLE_CLIENT_SECRET ${{secrets.GOOGLE_CLIENT_SECRET}}
            # yq w -i app.yaml env_variables.GOOGLE_CLIENT_ID ${{secrets.GOOGLE_CLIENT_ID}}
            # yq w -i app.yaml env_variables.PROJECT_ID ${{secrets.PROJECT_ID}}
      - name: Run deployment
        uses: actions-hub/gcloud@master
        env:
          APPLICATION_CREDENTIALS: ${{secrets.GOOGLE_APPLICATION_CREDENTIALS}}
          PROJECT_ID: ${{secrets.GCLOUD_PROJECT_ID}}
        with:
          args: app deploy app.yaml
